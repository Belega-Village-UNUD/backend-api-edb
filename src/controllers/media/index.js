const singleUpload = require("./single.controller");
const multiUpload = require("./multi.controller");
const uploadToImageKit = require("./upload.controller");

module.exports = {
  singleUpload,
  multiUpload,
  uploadToImageKit,
};
